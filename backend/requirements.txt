# FastAPI backend
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# TTS Engines
TTS>=0.22.0                       # XTTS2 voice cloning (Coqui)
kokoro>=0.9.4                     # Kokoro TTS (British voices)
transformers>=4.35.0,<4.45.0      # For TTS compatibility

# Audio processing
soundfile>=0.12.1
numpy>=1.24.0,<2.0.0

# PyTorch (CPU for macOS) - pinned for TTS compatibility
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.6.0

# Database
aiosqlite>=0.19.0

# Testing
requests>=2.31.0
httpx>=0.25.0
