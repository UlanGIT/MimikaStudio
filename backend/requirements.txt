# FastAPI backend
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# TTS Engines
TTS>=0.22.0                       # XTTS2 voice cloning (Coqui)
kokoro>=0.9.4                     # Kokoro TTS (British voices)
transformers==4.57.3              # Required by qwen-tts; XTTS patched for compat

# Audio processing
soundfile>=0.12.1
numpy>=1.24.0,<2.0.0
pydub>=0.25.1                     # MP3 conversion for audiobooks
scipy>=1.10.0                     # Audio resampling for speed adjustment

# PyTorch (CPU for macOS) - pinned for TTS compatibility
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.6.0

# Database
aiosqlite>=0.19.0

# Testing
requests>=2.31.0
httpx>=0.25.0

# CLI file format support
PyPDF2>=3.0.0                     # PDF text extraction
pymupdf>=1.24.0                   # Better PDF extraction (skip headers/footers)
python-docx>=1.0.0                # DOCX support
docx2txt>=0.8                     # Legacy DOC support
ebooklib>=0.18                    # EPUB support
beautifulsoup4>=4.12.0            # HTML parsing for EPUB

# Text processing
spacy>=3.7.0                      # Robust sentence tokenization (like audiblez)

# System monitoring
psutil>=5.9.0
